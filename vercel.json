{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/proxy/:path*", "destination": "https://computation-involves-rush-violation.trycloudflare.com/api/:path*" }
  ]
}
